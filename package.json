{
  "name": "qr-wasm",
  "version": "0.1.0",
  "description": "QR Code Generator powered by Rust/WASM on Cloudflare Workers Edge",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cargo build --release",
    "test": "cargo test",
    "test:unit": "cargo test --lib",
    "test:api": "bash tests/api_tests.sh",
    "test:all": "npm run test:unit && npm run test:api",
    "test:coverage": "mkdir -p test-reports && cargo tarpaulin --out Html --output-dir test-reports",
    "test:coverage:xml": "mkdir -p test-reports && cargo tarpaulin --out Xml --output-dir test-reports",
    "test:junit": "bash tests/generate_junit.sh",
    "test:reports": "npm run test:coverage && npm run test:api:junit",
    "test:api:junit": "bash tests/api_tests.sh --junit"
  },
  "keywords": [
    "qr-code",
    "wasm",
    "cloudflare-workers",
    "rust",
    "edge-computing"
  ],
  "author": "daiku-llc",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.56.0"
  }
}

